machine:
    python:
        version: 3.6.1
dependencies:
    override:
        - pip install -r requirements.txt
        - pip install coveralls
test:
    database: testbucketlist
    override:
        - nose2 --with-coverage
    post:
        - coveralls
deployment:
  staging:
    branch: backend
    commands:
      - git fetch origin --unshallow